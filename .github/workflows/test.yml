name: 👨🏻‍🔬 Test actions@ipfs

on: [workflow_dispatch]
jobs:
  publish:
    runs-on: ubuntu-latest
    name: Upload to IPFS
    steps:
      - name: mel ipfs action
        id: ipfs
        uses: melMass/actions@ipfs
      # Use the output from the `hello` step
      - name: Get the CID
        run: echo "The CID is ${{ steps.ipfs.outputs.cid }}"

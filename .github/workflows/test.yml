name: 👨🏻‍🔬 Test actions@build-python-wheels

on: [workflow_dispatch]
jobs:
  publish:
    runs-on: ubuntu-latest
    name: Build python wheels
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Build python wheels
        id: build
        # uses: melMass/actions@build-python-wheels
        uses: ./
        with:
          wheels: "misaka"
      # - name: Print wheel paths
      #   run: |
      #     IFS=$'\n' read -d '' -r -a wheel_paths <<< "${{steps.build.outputs.wheel_paths}}"
      #     echo -e "\e[1;32mThe paths to the built wheels are:\e[0m"
      #     for path in "${wheel_paths[@]}"; do
      #       echo -e "\e[1;34m- $path\e[0m"
      #     done
      #   shell: bash
